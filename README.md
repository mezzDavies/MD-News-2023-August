# Northcoders News API

## Instructions

After cloning this repo you will need to create two .env files at root level: .env.test and .env.development. Into each, add PGDATABASE=, with the correct database name for that environment (see /db/setup.sql for the database names).
